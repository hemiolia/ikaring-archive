# 開発指示

- 開発ログ.mdと残タスク総覧.md、docs/design/設計.mdを最初に読む。中断復帰時も再読する。
- 対戦だけでなくバイトを含め、取得対象の網羅性と保存する全項目の完全性を別々に検査する。
- 不明点は範囲を推測せず質問。取得できていないものを取得済みと書かない。
- ソースと実データを別ディレクトリに保存。秘密、実戦績、DB、スプール、ローカルログをcommitしない。
- 通常実装と単純作業はagyのgemini-3.8-flash-highへ精密な仕様で委譲。Geminiに判断をさせない。QUESTIONでAstraへ戻す。
- Astraは設計、オーケストレーション、難しい実装、独立検証を担当。Gemini枠切れ時はTerraまたはLuna(effort=max)。
- 既存版を保存し、依頼は原文ママで開発ログに残す。未完了を残タスク総覧から落とさない。
- 検証: npm test。認証なしのテストにライブAPIアクセスを混ぜない。公開サンプルと本人データを混同しない。
- 絵文字を使用しない。応答と文書は日本語。
