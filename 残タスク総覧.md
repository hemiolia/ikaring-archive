# 残タスク総覧

最終更新: 2026-09-22。依頼原文は開発ログ.md。完了判定の正典は実測結果。

## 完了

- 既設nxapiと認証保存先を確認。その時点では未ログイン。その後、本人ログインは完了し、セッショントークンの有効期間は730日と実測した。
- 現行イカリング3から113操作定義を抽出。読み取り104経路の引数を解決。
- 対戦・サーモンラン・イベント・個人実績・サイドオーダー等を対象にする汎用収集計画を実装。
- 原文応答・履歴改版・永続取得キュー・関連IDと追加ページ・SQLite検索ビューを実装。
- 開発専用ディレクトリと出力専用ディレクトリを分離。

## 実施中

1. 残っている取得ジョブを進め、ナワバリ、イベマ、Xマッチ、フェス、プラベ、バイト、ビッグラン、バイトチームコンテストが実データに現れるか確認する。現在の詳細はオープン38、チャレンジ1だけ。
2. 実行中の同期が終わったあと、レート系列が実データに出るか確認し、`gui` を作り直す。コード上の抽出と画面は単体テスト済み。実行中の同期は古いプロセスなので、次の起動で表 `rate_points` が入る。
3. GitHub Actions を置くには、今の認証に workflow scope が無い。定義は `docs/operations/github-actions-tests.yml` にある。
4. ターミナルを知らない人でも導入できる形。設計は後でよい、と前田さんが言っている。未着手。ここを忘れて公開完了にしない。
## 完了（公開）

- https://github.com/hemiolia/ikaring-archive は PUBLIC。main を push した。秘密と実データの sqlite は含めていない。
- LaunchAgent `local.ikaring3.archive` は登録済みで、同期プロセスが動いている。再起動後の再開は、まだ再起動していないので未確認。

## 完了（分析表）

- `export-xlsx` はジャンル別シートの `分析.xlsx` を作り直す。正本は SQLite。編集は戻さない。原文は入れない。実測はオープン38、チャレンジ1、ほか0。

## 認証

- 再ログインが要るのはセッショントークンの期限切れか失効だけ。2026-09-22実測で有効期間は730日。
- bulletは約2時間、NSO資格情報は7200秒、短いアクセストークンは900秒。これらはセッショントークンから更新する。
- 認証失敗を成功扱いしない。通知と AUTH_INCIDENT を残す。

## 本人のログイン後に必要（未完了）

- ニンテンドーアカウントの本人ログイン。
- 実アカウントで全取得経路、全関連詳細、追加ページ、画像バイナリの初回収集。
- 記録がない状態、サービスが公開しない記録、取得エラーを実応答で区別。
- 画面上の対戦・バイト・実績とDBの照合。実データを取り込む前に「すべて取得済み」と断定しない。
- 現行サーバーが返さない過去記録・内部記録まで取り出せるとの保証はできない。取得不能範囲は監査結果として明記する。

## 継続保守

- アプリ更新時のクエリ定義再取得と未対応引数の検出。
- 履歴が取得窓から流出する前の定期収集、失敗と長時間未同期の監査。
- SQLiteバックアップと復元検証。
