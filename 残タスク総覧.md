# 残タスク総覧

最終更新: 2026-09-25 16:50。依頼原文は開発ログ.md。完了判定の正典は実測結果。記載の件数・サイズ等の数値は当時のスナップショットであり、過去の記録である（最新値ではない）。

## 最優先：Fuguから引継いだ正本移行（2026-09-25）

- 詳細な再開位置は `docs/operations/astra-continuation-20260925.md`。移行完了後も、ユーザーの停止指示まで既存承認済み残タスクを自律選定して開発を続ける。
- 2026-09-25 16:50 JST 自動再開marker `3ecee30b7d76a03a1c08c35c` が task_started turn `01a0d78b-1406-7202-882e-7126df528ee3` と対応し配送確認。watcher active、last exit 0。
- **最新NAS再転送・NAS collector起動・正本切替・実DBのGoogle Drive全量復元検証・ローカル巨大DB削除は依然として未完了。** 保全検証前のローカル削除は禁止を維持。
- NAS ssh接続は依然 bad handshake（16:50は外部経路を再測定。宅内到達不能は11:50の測定）。
- 記録流出防止のためMac collectorはactive稼働中。全7履歴current、auth正常、exports更新をstatus実測で確認。過去静止点（過去計測値: 06:50の26,218,319,872 bytes、11:59の26,287,431,680 bytes等）以降もローカルが更新されているため、NAS復帰後には再停止と最新静止点の再転送が必要。
- 旧22GB snapshot（`backups/astra-resume-20260922-cow.sqlite3`）: 暗号化完了。receiptはデータ側 `logs/development/astra-local-old-encryption-20260925.json`。
  - raw SHA256: `1308b6786c2a82aaf96a137d0190ee9c346feb0dd53043caf5c8e0ee7d40bdbc`
  - cipher: 1,762,497,867 bytes、SHA256: `8f5b294d4a129ecd5759ded2a92aa59a9d8026fa0ab0ce5ae5f1e4e820ce9aa6`
- Google Drive保全: connector uploadは 536,870,912 bytes（512MiB）上限により転送開始前に拒否。
  - Astra決定により 256MiB chunk + manifest 復元方式を Sol へ実装委譲中。実uploadは未完了。
- 11:59静止clone（`backups/astra-latest-20260925_115900.sqlite3`、過去計測値 26,287,431,680 bytes）: 16:51台に暗号化開始（session 99706）、完了未確認。
- Sol委譲タスク: 同期実transport修正とquick_check 64MiB cache設定済み。Sol報告の `npm test` は Python 152、Node 3 成功（過去の139件から進捗）。Astra独立実行でもPython152件・Node3件が成功（/tmp/ikaring-tests-20260925_1654.log）。
- 質問3件は未回答のまま保留:
  1. NAS到達不能状況（宅内IPおよび外部トンネルの確認状況）
  2. Google Drive日次無人アップロード用の書込みOAuth認可
  3. GitHubに暗号化DB自体の保管も必須か、既存の公開ソース・復旧手順という役割でよいか




### 21:52以降の更新（上記16:50の状態から進展）

- 旧22GBスナップショットは全27片をGoogle Driveへ転送し、全量読み戻し・復号後の22,264,725,504 bytesとSHA256一致を実測。2manifestもupload/読み戻し一致。データ側 `logs/development/astra-old-cloud-complete-20260925.json` はstatus=complete。
- connectorには512MiBと100MiBの二段上限があり、実転送片を64MiBへ変更した。11:59静止点は暗号化・ローカル全量復号検査済み、36片のcloud uploadが進行中。最新の転送IDは `astra-latest-cloud-parts-20260925.json`。このsnapshotのcloud全量復号検証は未完了。
- 21:52でもNAS外部SSHは接続不能。Macは全7履歴current、認証正常。NAS復帰時にはさらに新しい静止点を再作成する。
- 独立npm testはPython161件・Node3件成功。Solが人工DB+fake rcloneの実subprocessで日次backup cycleを完走しreceipt生成まで確認。これは実NAS/cloud日次稼働の証明ではない。
- private session transcriptと継続台帳を公開対象から除外してローカル保持。公開前監査は継続、未commit/push。元DB/静止snapshot/暗号backupは保持。


## 完了

- 既設nxapiと認証保存先を確認。その時点では未ログイン。その後、本人ログインは完了し、セッショントークンの有効期間は730日と実測した。
- 現行イカリング3から113操作定義を抽出。query 106経路のうち、記録外2経路と完全詳細の厳密な部分集合1経路を除く、独立した読み取り103経路の引数を解決。
- 対戦・サーモンラン・イベント・個人実績・サイドオーダー等を対象にする汎用収集計画を実装。
- 原文応答・履歴改版・永続取得キュー・関連IDと追加ページ・SQLite検索ビューを実装。
- 開発専用ディレクトリと出力専用ディレクトリを分離。

## Astra継続中の最優先（2026-09-22夜）

- 最新依頼原文: 開発ログv7。オープンとイベント双方を本人参加回だけ、Xランキングは終了シーズン最終だけ。オープンをイベントへ置換しない。
- 最新詳細340件（318から22増）、最新試合16:52:16。取得不能10件は別扱い。20:31台に全7履歴を自動再確認、GUI/xlsxも自動更新。
- Fugu未完了差分を保持し、null応答の誤分類・監査削除を修正。Python99件・Node3件成功。公開反映前の最終検証中。
- ランキング制限はcollectorへ統合済み。イベント1000期間のうち保存済み参加証拠がある5件を対象、995件をout_of_scope。証拠追加で再有効化する。オープンの本人戦績・パワー・参加表彰は維持。
- 開催中の取得を最終版に誤認しないよう、終了後取得の証拠を要求する修正とcollector統合負例テストを追加中。4ルール複合操作の279項目は代替5操作に全数存在。
- XLSX相関SQLを高速化し実測0.60秒、GUI0.25秒。時刻ローカル化・全7経路の最古確認・容量/認証警告は実装済み。Chrome1280/390/320px、フォントloaded・外側横溢れ0・JS例外0を確認。
- 容量: 稼働DB約21GB。低容量保護とバックアップ事前容量検査を実装。編集前APFS複製はデータ側backups/astra-resume-20260922-cow.sqlite3。quick_check=ok、matches350。
- NAS 192.168.11.10:2222、/home/Natsuki/ikaring-archive/backupsへ上記複製22,264,725,504 bytesを転送中（exec session89966）。完了後にサイズ・SHA-256・SQLiteを検証しローカル参照を作る。検証前に複製を削除しない。稼働DB自体の容量削減・外置きは未完了。バックアップ移動と混同しない。
- 作業ツリー未コミット。残る検証→実運転→個人データ混入検査→commit/push。

## 復旧後の優先課題（2026-09-22）

- 同一応答の重複排除でジョブ完了更新まで飛ばす不具合を修正。修正後の手動同期run27で未保存17試合を回収し、318試合すべてに詳細が付いた。最新は2026-09-22 10:58:47 JSTのバンカラマッチ（チャレンジ）。次のLaunchAgent自動実行と、以後の取得・生成物更新を実測する。
- Fugu履歴から未記帳の追加指示を回収。正式名称・公式順序、評価ポイント0〜999、欠字フォールバック、所持ウデマエポイントが取得できないことの表示、誤った勝敗由来チョーシの撤回とvibes取得を実装中。実画面・全テストの最終確認後に公開へ反映する。
- 初回収集の残り経路、画像、特殊バイト、一覧にあるが詳細が取れない記録の監査を続ける。以下の前回件数は過去スナップショットであり、最新の数はstatus/auditで確認する。

## 実施中

1. 残っている取得ジョブと画像を進め、フェス、プラベ2対2、ビッグラン、バイトチームコンテストが実データに現れるか確認する。最新スナップショットでは、ナワバリ50、オープン49、チャレンジ1、イベマ50、Xマッチ50、プラベ4対4が27、3対3が19、1対1が2、その他2、通常バイト50。上記4区分は0で、記録不存在と未取得をまだ確定区別していない。終了済みジョブ350、未了421、画像取得済み140・未了2811。
2. 残りの取得が進むたび、ビッグラン、バイトチームコンテスト、ブキチャレパワー等の未出現系列が実データに現れるか確認する。`rate_points` とレート画面の実データ生成は完了したが、記録が無い系列を取得済みとはしない。
3. GitHub Actions を置くには、今の認証に workflow scope が無い。定義は `docs/operations/github-actions-tests.yml` にある。
4. ターミナルを知らない人でも導入できる形。設計は後でよい、と前田さんが言っている。未着手。ここを忘れて公開完了にしない。
5. 全機能を扱う統合GUI。収集状態、認証事故、取得監査、対戦・バイト一覧と詳細、検索・絞り込み、タグ、分析、書き出し、バックアップを一つの利用体験へ統合する。現在の生成HTMLはレート確認画面であり、このタスクの完了ではない。
6. スマートフォンアプリとしてのリリース。SQLite正本を直接配布せず、デスクトップと共通のアプリケーション層を使う。PCとの接続・同期方式、認証、暗号化、外部公開の有無、ストア配布要件は未設計。
## 完了（公開）

- https://github.com/hemiolia/ikaring-archive は PUBLIC。main を push した。秘密と実データの sqlite は含めていない。
- LaunchAgent `local.ikaring3.archive` は登録済みで、同期プロセスが動いている。再起動後の再開は、まだ再起動していないので未確認。

## 完了（分析表）

- `export-xlsx` はジャンル別シートの `分析.xlsx` を作り直す。正本は SQLite。編集は戻さない。原文は入れない。実測はオープン38、チャレンジ1、ほか0。
- `gui` はSplatoon2 UnifiedをHTMLへ完全埋め込みし、NOAHS配色のまま縦横軸、Y目盛り、日付目盛り、補助線、ゼロ線、面、点、系列要約を持つ。デスクトップと狭幅表示を実画像で確認した。

## 認証

- 再ログインが要るのはセッショントークンの期限切れか失効だけ。2026-09-22実測で有効期間は730日。
- bulletは約2時間、NSO資格情報は7200秒、短いアクセストークンは900秒。これらはセッショントークンから更新する。
- 認証失敗を成功扱いしない。通知と AUTH_INCIDENT を残す。

## 本人のログイン後に必要（未完了）

- 本人ログインは完了済み。以下は認証後の網羅性検証として継続する。
- 実アカウントで全取得経路、全関連詳細、追加ページ、画像バイナリの初回収集。
- 記録がない状態、サービスが公開しない記録、取得エラーを実応答で区別。
- 画面上の対戦・バイト・実績とDBの照合。実データを取り込む前に「すべて取得済み」と断定しない。
- 現行サーバーが返さない過去記録・内部記録まで取り出せるとの保証はできない。取得不能範囲は監査結果として明記する。

## 継続保守

- アプリ更新時のクエリ定義再取得と未対応引数の検出。
- 履歴が取得窓から流出する前の定期収集、失敗と長時間未同期の監査。
- SQLiteバックアップと復元検証。
